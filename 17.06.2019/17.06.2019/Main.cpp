#include<iostream>
#include "Header.h"

using namespace std;

int main() {

	List list;

	list.showList();
	//list.AddElement(22);
	list.showList();
	//list.AddElement(11);
	list.showList();

	system("pause");
	return 0;
}